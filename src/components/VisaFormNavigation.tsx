import { FormConfig, VisaProgress } from '@lib/domain/form'

export function VisaFormNavigation({
  config,
  progress,
}: {
  config: FormConfig
  progress: VisaProgress
}) {
  return <>sidebar</>
}
